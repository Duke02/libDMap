package com.duke.maps;

/**
 * Enum that contains all available tiles for library. 
 * Is not extendable and would need to be worked on so that it is.
 * @author Duke
 *
 */
public enum Tile {

	WALL,
	FLOOR,
	BOUNDS;
}
